class NoteModel {
  NoteModel({
    this.id,
    this.noteTitle,
    this.noteDescription,
    this.noteImagesPaths,
  });

  int? id;
  String? noteTitle;
  String? noteDescription;
  String? noteImagesPaths;
}
